---
"facturas": patch
---

Point the README links at facturas-sdk.dev, fill in the package homepage, author and keywords, and document every error code, including `ArcaInvalidSoapResponseError` and the `ArcaInputError` codes.
